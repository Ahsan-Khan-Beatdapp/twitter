name: Auto Create PR to Production

on:
  push:
    branches:
      - main

jobs:
  create-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup Git
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'

      - name: Create Pull Request
        uses: repo-sync/pull-request@v1
        with:
          source_branch: "main"
          destination_branch: "production"
          pr_title: "Merge main into production"
          pr_body: "Auto-generated PR to merge main into production"
          pr_reviewer: "username1,username2" # Optional: Add reviewers if needed
          pr_label: "automerge" # Optional: Add labels if needed
          github_token: ${{ secrets.GITHUB_TOKEN }}
